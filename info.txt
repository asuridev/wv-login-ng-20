import test from 'node:test';
import assert from 'node:assert/strict';

const appUrl = process.env.APP_URL;

if (!appUrl) {
  throw new Error('APP_URL no está definida');
}

test('Check application URL', { timeout: 60000 }, async () => {
  const response = await fetch(appUrl);
  assert.equal(response.status, 200);
});


"acceptance-test": "mkdir -p reports && node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=reports/acceptance-test-results.xml acceptance-test/"


"acceptance-test": "node -e \"require('fs').mkdirSync('reports',{recursive:true})\" && node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=reports/acceptance-test-results.xml acceptance-test/"
